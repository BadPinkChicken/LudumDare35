#pragma once

#include "data.hpp"

class   Background
{
private:
sf::RectangleShape      *background;
sf::RectangleShape      *ground;
sf::Texture             backTex;
sf::Texture             groundTex;

public:
    Background();
    BackGround(const std::string & back, const std::string & ground, sf::Vector2f sizeB, sf::Vector2f sizeG);
    sf::RectangleShape  &getBack();
    sf::RectangleShape  &getGround();
    void                update(sf::RenderWindow &);
};
